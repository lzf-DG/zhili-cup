# 项目摘要（2026-08-29）

> 供新会话继续开发的项目上下文。仓库根：`C:\Users\yxy\Desktop\智理杯`，分支 main，远程 `https://github.com/lzf-DG/zhili-cup.git`。

## 1. 项目目标

「Pre大师」答辩模拟系统（"智理杯"智能体大赛作品）：学生输入答辩主题、上传 .pptx，
在课堂风格界面中与三位 AI 评委（王教授-严格 / 李教授-温和 / 张同学-好奇）进行
汇报 + 一对一问答模拟，最后生成复盘评分报告。支持接入真实 LLM API（OpenAI 兼容），
未配置时使用内置 Mock 评委。

## 2. 当前目录结构

```
智理杯/
├── pre-master-app/            # 主应用（Vite + React）
│   ├── src/
│   │   ├── App.tsx            # 阶段状态机：idle → reporting → Q&A → finished
│   │   ├── main.tsx
│   │   ├── agents/
│   │   │   ├── types.ts       # Agent / ChatMessage / ReportData 类型
│   │   │   ├── mockAgents.ts  # 3 评委定义 + 内容感知 Mock 问答 + 跨评委去重
│   │   │   ├── apiAgent.ts    # 真实 API 调用（经后端代理）+ 配置验证
│   │   │   └── agentManager.ts
│   │   ├── components/
│   │   │   ├── WelcomeScreen / JudgeRow / DialogBox / Timer / MicButton
│   │   │   ├── AnswerBox / TextInput / SettingsModal / ReportView
│   │   │   ├── PptViewer.tsx          # 投影容器：结构化 → 图片 → 文本 三级回退
│   │   │   └── PptStructuralViewer.tsx# 结构化渲染（绝对定位还原 PPT 布局）
│   │   ├── hooks/
│   │   │   ├── useChat.ts             # 会话主逻辑（汇报/答辩阶段、API→Mock 回退）
│   │   │   └── useSpeechRecognition.ts# Web Speech 语音识别 + 端点检测
│   │   ├── store/sessionStore.ts      # zustand：消息/阶段/报告/PPT 状态
│   │   └── utils/
│   │       ├── pptStructure.ts        # pptx-parser 结构化解析 + 单位/颜色/实体工具
│   │       └── pptParser.ts           # 回退模式：JSZip+DOMParser 提文本/图片
│   ├── server/index.ts        # Express 后端（端口 3001，见 §6）
│   ├── public/                # 静态资源、recorder.html、uploads/
│   ├── scripts/generate-test-ppt.cjs # 测试用 PPT 生成
│   ├── vite.config.ts         # /api 代理到 3001
│   └── package.json           # dev = vite + tsx server 并发
├── .claude/                   # Claude 会话配置
└── *.docx / screenshot_*.png  # 大赛材料、界面截图（勿动）
```

## 3. 完成的功能

- 欢迎页（主题输入 + PPT 上传）→ 汇报阶段 → 答辩阶段 → 复盘报告的完整流程
- 三位评委人格化问答：Mock 评委**内容感知**（先点评回答再按关键词追问；含糊回答
  要求澄清；用户反问时评委解释自己指什么，实现一对一深聊）
- **跨评委提问去重**：全局记录已问问题，按"被问次数 + 字符二元组 Jaccard 相似度"降权，避免逐字/极近似重复
- 汇报阶段特殊处理：评委不即时评价，点「结束汇报」后才反馈；答辩阶段隐藏该按钮
- 语音输入（Web Speech API，自动端点检测已延长至原 5 倍）+ 文字输入 fallback
- PPT 呈现三级回退：结构化渲染 → 整页图片 → 纯文本
- **PPTX 结构化渲染**（pptx-parser 解析 + 绝对定位还原）：文本框/图片/形状、
  旋转翻转、填充/描边、对齐/锚点、字号/行距/字距、颜色
- 复盘报告：五维评分（总体/逻辑/内容/表达/时间）+ 亮点/改进 + 对话摘要
- 真实 API 接入：SettingsModal 配置 Base URL / Key / 模型，**保存时真实发请求验证**，
  显示 配置成功(✓自动关闭) / 配置失败(✗保留原因)；前端经后端代理调用，Key 不落浏览器到第三方

## 4. 正在开发的功能

- 无进行中的任务。最近一次修复（PPTX 渲染稳定性）已完成并推送（HEAD `c3bdb6b`，与 origin/main 同步）：
  1. 新增 `decodePptxText`：解码 pptx-parser 残留的 `&#10;`/`&#xA;`/`&amp;` 等 XML 字符引用（`&amp;` 最后解码防二次解码；10/11/12/13→`\n`）
  2. 修正字号：`fontSize.value` 已是 px(96dpi)，去掉错误的 `×96/72`（原先文字偏大 33%）
  3. 字体回退栈：原始字体名加引号 + CJK/sans 回退，图标字体缺失不再排版崩坏
  4. 应用解析出的 `lineSpacing`（PERCENTAGE 倍率 / PX 固定值）与 `letterSpacing`（字号百分比），带越界保护

## 5. 关键技术

- Vite 8 + React 19 + TypeScript 6（`tsc -b` 构建，验证用 `npx tsc --noEmit`）
- zustand 全局状态；framer-motion 动效；内联 style 风格（金色 #FFD54F、深蓝渐变、rgba）
- Web Speech API 语音识别（浏览器端）
- pptx-parser@1.1.7-beta.9：浏览器 UMD bundle，`main:"dist"` → dist/index.js，
  需防御性解包 `mod?.default?.default ?? mod?.default ?? mod?.parse ?? mod?.vf ?? mod`；
  调用 `parse(file, { flattenGroup: true })`。
  **单位约定（parser 已换算）**：position/size/fontSize.value 均为 px@96dpi；
  lineSpacing：PERCENTAGE=小数倍率 / PX=固定行高；letterSpacing=相对字号的百分比小数
- 本地 Express 后端代理（端口 3001）转发 LLM 请求，避免 CORS 与 Key 暴露
- 回退 PPT 解析：JSZip 解 ZIP + 浏览器 DOMParser 解析 XML（DOMParser 会正确解码实体，回退模式无 `&#10;` 问题）

## 6. 重要文件说明

| 文件 | 说明 |
|---|---|
| `src/App.tsx` | 阶段路由；主界面布局（评委席/投影/对话区/底部操作栏） |
| `src/hooks/useChat.ts` | 会话核心：startSession / sendMessage(汇报阶段仅记录) / endReport / endSession / restart；API 调用失败自动回退 Mock |
| `src/agents/mockAgents.ts` | Mock 评委全部内容感知逻辑、去重状态（`askedQuestions`，`resetMockState` 清空） |
| `src/agents/apiAgent.ts` | callApi / generateReportViaApi / **validateApiConfig**（最小请求验证）/ mapApiError（502→401/404/429，503→未收到配置） |
| `src/utils/pptStructure.ts` | parsePptxStructure（UMD 解包）/ toCssColor / **decodePptxText** / PT_TO_PX；头部注释有完整单位约定 |
| `src/components/PptStructuralViewer.tsx` | usePptxStructure hook + StructuralSlide（ResizeObserver 测宽算 scale）+ ElementView + renderText（解码/字号/字体回退/行距字距） |
| `src/components/PptViewer.tsx` | 三级回退选择：结构化 → 图片 → 文本；投影框 58% 宽 |
| `src/utils/pptParser.ts` | 回退模式文本/图片提取（DOMParser，勿与结构化模式混淆） |
| `server/index.ts` | 后端 3001：`POST /api/chat`、`POST /api/report`（转发 LLM，服务端拼 Bearer）、`POST /api/convert-ppt`、`/uploads` 静态 |
| `vite.config.ts` | `/api` 代理 → `http://localhost:3001` |

## 7. 已知问题

- **录音无硬上限**：语音端点检测延长后，超长录音仍依赖浏览器自身限制，未做前端强制截断（用户未确认是否需要，**不要主动实现**）
- pptx-parser 局限：自定义 XML 解析器不解码实体（已在应用层 `decodePptxText` 补偿）；渐变填充/图片填充/复杂图形路径仅做了基础处理，复杂 PPT 可能仍有失真
- 图标字体（Wingdings/Webdings/Segoe UI Symbol 等）依赖本机安装，缺失时显示基础字符而非原图标（已加回退栈，无法进一步还原）
- pptx-parser 是 UMD bundle，动态 import 解包逻辑脆弱，升级版本时优先验证 `parse is not a function` 是否复现

## 8. 下一步要做什么

- 等待用户实测 PPTX 渲染修复效果（重新打开之前出 `&#10;` 的 PPT 验证）
- 若用户反馈仍有渲染问题：优先检查 pptStructure.ts 头部注释中的单位约定与 renderText 的防护分支
- 可选（需用户确认后再做）：录音时长硬上限；渐变/图片填充的渲染增强

## 9. 不能改动 / 需要注意的约束

- **安全（最高优先级）**：永不索要/接收用户的 GitHub 密码（GitHub 2021 起已禁用 HTTPS 密码）；API Key 是机密——存于 localStorage `api_key`，仅经后端 `/api/chat`、`/api/report` 转发，**不得打印日志或暴露到 UI**
- **Git 工作流**：用户说「同步git / 提交git」时才 commit + push；其他任何情况**不要提交**。远程为 HTTPS，push 偶发网络失败时重试一次即可
- 不要修改 `node_modules/pptx-parser/dist/`（只读分析对象）
- 类型检查验证命令：`cd pre-master-app && npx tsc --noEmit`
- 汇报与提示文案用**中文**
- 汇报阶段 / 答辩阶段的行为差异（评委不即时评价、按钮显隐）是用户明确要求的，改动 useChat/App 时勿破坏
- 根目录的 .docx 材料与 screenshot_*.png 是大赛交付物，勿删改
